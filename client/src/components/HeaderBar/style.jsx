import { makeStyles } from '@material-ui/core/styles';

 const HeaderBarStyle = makeStyles((theme) => ({
  headerBar: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
  },
}));

export default HeaderBarStyle;